// Thực hiện việc booking vé

// Khách hàng phải đăng nhập thì mới cho vào trang này, còn không thì redirect họ về trang đăng nhập
import React from 'react';

const Booking = (props) => {
  return <div>Booking Page</div>;
};

export default Booking;
